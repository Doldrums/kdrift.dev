{
  "name": "kdrift-web",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "pnpm run build && wrangler dev",
    "deploy": "pnpm run build && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.28.0",
    "vite": "^6.3.5",
    "wrangler": "^4.73.0"
  },
  "dependencies": {
    "@amplitude/unified": "^1.0.8"
  }
}
